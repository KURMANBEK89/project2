package meatProducts;

import java.time.LocalDate;

public class Fish extends MeatProducts{
    public Fish(int id, String name, String company, LocalDate date) {
        super(id, name, company, date);
    }
}
